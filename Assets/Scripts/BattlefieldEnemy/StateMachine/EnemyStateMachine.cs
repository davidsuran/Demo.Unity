using Assets.Scripts.Battlefield.Data.StateMachine;

namespace Assets.Scripts.Battlefield.Enemy.StateMachine
{
    public class EnemyStateMachine : Data.StateMachine.StateMachine
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}